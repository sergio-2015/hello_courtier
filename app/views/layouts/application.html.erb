<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title).blank? ? "hello-courtier.com" : yield(:title) %></title>
    <meta name="description" content="<%= (yield(:description) || "").squish %>">
    <%= csrf_meta_tags %>

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <%= favicon_link_tag 'logo_picto.png' %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
  </head>
  <body>
    <%= render 'shared/navbar' %>

    <%= yield %>
    <%= render 'shared/flashes' %>
    <%= render 'shared/footer' %>

    <div class="modal fade" id="modal_broker_signin" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
      <div class="modal-dialog" role="document">
        <div class="modal-content" id="modal_broker_signin_div">
          <div class="modal-body">
            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <div class="broker_signin">
              <div class="broker_signin-title">
                <h4>Accès courtier - connexion</h4>
              </div>

              <div class="broker_signin-form">

                <%= render "devise/sessions/new_session" %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>


    <%= javascript_include_tag "https://maps.google.com/maps/api/js?libraries=places&sensor=false" %>
    <%= javascript_include_tag "https://cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js" %>

    <%= javascript_include_tag 'application' %>
    <%= yield(:after_js) %>
  </body>
</html>
